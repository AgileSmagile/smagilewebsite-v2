# Cloudflare Web Analytics (cookie-free, always on)
# Get your token from: Cloudflare Dashboard > Web Analytics > Add a site > smagile.co
PUBLIC_CF_ANALYTICS_TOKEN=

# Google Analytics (consent-gated, only loads after user opts in)
# Get your measurement ID from: Google Analytics > Admin > Data Streams
PUBLIC_GA_MEASUREMENT_ID=

# FreeAgent OAuth (for /admin/finance/ integration, read-only)
# Get credentials from: FreeAgent > Settings > Developer Dashboard > OAuth App
FREEAGENT_CLIENT_ID=your_oauth_identifier
FREEAGENT_CLIENT_SECRET=your_oauth_secret

# Supabase (smagile website DB - job opportunities, leads, etc.)
# Get credentials from: Supabase Dashboard > Project Settings > API
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Mosaic CV Supabase (separate project - for profile matching in emergency search)
# Get credentials from: Mosaic Supabase project > Settings > API
MOSAIC_SUPABASE_URL=
MOSAIC_SUPABASE_SERVICE_ROLE_KEY=

# Admin access control (comma-separated email addresses)
# Users authenticated via Cloudflare Access whose email must match to access /admin/*
ADMIN_EMAILS=
OWNER_EMAIL=

# Businessmap (for opportunity card creation on Board 5)
# Same key as sonnet-agent/.env BUSINESSMAP_API_KEY
BUSINESSMAP_API_KEY=
